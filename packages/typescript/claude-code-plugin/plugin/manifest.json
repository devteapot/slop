{
  "dxt_version": "0.1",
  "name": "slop",
  "version": "0.1.0",
  "description": "Observe and control desktop and web applications via SLOP. Discover local apps and browser tabs, view their state, and perform actions.",
  "author": {
    "name": "SLOP AI"
  },
  "server": {
    "type": "node",
    "entry_point": "server/index.js",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/server/index.js"],
      "env": {
        "CLAUDE_PLUGIN_DATA": "${CLAUDE_DATA_DIR}"
      }
    }
  },
  "tools": [
    {
      "name": "connected_apps",
      "description": "List and inspect SLOP-enabled applications"
    },
    {
      "name": "app_action",
      "description": "Perform a single action on an application"
    },
    {
      "name": "app_action_batch",
      "description": "Perform multiple actions on an application in one call"
    }
  ]
}
