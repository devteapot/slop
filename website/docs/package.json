{
  "name": "docs",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "generate:content": "node ./scripts/generate-content.mjs",
    "check:content": "node ./scripts/generate-content.mjs --check",
    "dev": "bun run generate:content && astro dev",
    "start": "bun run generate:content && astro dev",
    "build": "bun run generate:content && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.38.2",
    "astro": "^6.0.1",
    "sharp": "^0.34.2"
  }
}
