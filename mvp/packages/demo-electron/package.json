{
  "name": "demo-electron",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.mjs",
  "scripts": {
    "build": "bun run build.ts",
    "start": "bun run build && electron dist/main.mjs",
    "dev": "bun run build && electron dist/main.mjs"
  },
  "dependencies": {
    "@slop/provider": "workspace:*",
    "@slop/types": "workspace:*",
    "electron": "^36.0.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.18.0"
  }
}
