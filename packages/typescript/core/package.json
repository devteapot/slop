{
  "name": "@slop-ai/core",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": ["src", "dist"],
  "sideEffects": false,
  "license": "MIT",
  "description": "SLOP client library — let AI observe and interact with your app's state",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --format esm && bunx tsc --emitDeclarationOnly --outDir dist",
    "clean": "rm -rf dist"
  }
}
